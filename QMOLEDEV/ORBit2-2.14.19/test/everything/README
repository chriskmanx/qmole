This is a port of the orbitcpp 'everything' test to ORBit.

The server writes the ior of the test factory out to the file
'iorfile', which the client then reads.

The 'test.sh' script runs the server, then client. It is invoked by
the 'make check' makefile target.

- Phil Dawes (philipd@users.sourceforge.net)
