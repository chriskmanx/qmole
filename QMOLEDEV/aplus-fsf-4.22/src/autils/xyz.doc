

				xyz
				===
		

0.  Description.

	XYZ is a gadget for displaying detail items of a dense (table)
	representation in sparse (matrix) form.

1.  Operation.

	E.g.,	
			$load s
			$load t
			$load xyz

			$cx p

			aû1 2 2 0 0
			bû4 0 1 2 1
			cû0 0 0 1 1
			dû1 0 0 1 0
			eûÉ#a
			fû10«É#a
			gû100«É#a

			$cx .
	
			`p t.open ()		ã open table in p
			xyz.define `		ã define gadget in root

			_CXû`p			ã context of table

		ã  Conceptual a«b table:

			_YYû0 1 2		ã Y-axis (row) order
			_XXû0 1 2 3 4		ã X-axis (col) order
			_Yû`a			ã Y-axis (row) field
			_Xû`b			ã X-axis (col) field
			_Zû`e`f			ã Z-axis fields

			_GOû1			ã re-evaluate

			show `._XYZ		ã display _XYZ

			_Zû`f`g`e		ã redefine z-axis
			_GOû1			ã trigger

			p.eûî¡p.e		ã mix datatypes
			_GOû1			ã trigger

		ã Conceptual (a c)«(b d) table:

			_YYû(0 1 2;0 1)		ã Y-axis (row) orders
			_XXû(0 1 2 3 4;0 1)	ã X-axis (col) orders
			_Yû`a`c			ã Y-axis (row) fields
			_Xû`b`d			ã X-axis (col) fields
			_Zû`e`f			ã Z-axis fields

			_GOû1			ã re-evaluate

2.  zûdefine cx

	cx = target context of XYZ gadget
	z = `cx._XYZ

	Define an XYZ-gadget in the specified context.

3.  Variables

	_GOû		1 (evaluation trigger)
	_YYû		Y-axis orders
	_XXû		X-axis orders
	_Yû		Y-axis fields
	_Xû		X-axis fields
	_Zû		Z-axis fields
	_CXû		context of table
	_Iû		permutation vector
	_Mû		matrix (simple or nested)
	_XYZû		outer layout

4.  Restrictions

	Each of _YY, _XX, _Y, _X, _Z must be non-null.

	XYZ uses the set- and preset-callbacks attached to _GO.



