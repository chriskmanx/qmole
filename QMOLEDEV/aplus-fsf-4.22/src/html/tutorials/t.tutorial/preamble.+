
directoryû"/usr/local/aplus-fsf-4.22/doc/tutorials/t.tutorial/files/"

ã Load t.

	$load t

ã Utilities.

	pair{x;y}:(x;y)

	out_fn{s;d;i;p;c;v}:{if ((©d)Å`int`float) {
			if ((dÅ¢Inf Inf)©d=(©d)Øt.NA) ' ' else 1Õîd
			    } else d}
	
	on_screen{table;fields;widths}:{
		(%`Ötable)ûtableÖfields;
		(`Ötable) is `table;
		(`Ötable) has (`titlefont;'helvetica-bold-14');
     		(%`Ötable) has¡<(`titlefont;'helvetica-10');
     		(%`Ötable) has¡<(`font;'helvetica-10');
		(`Ötable) has (`iconic;0);
		if (0¨#widths) (%`Ötable) has¡  pair¡{`space;Ówidths«10ß14};
		(%`Ötable) has¡  pair¡{`title;fields};
		(%`Ötable) has¡  <(`out;out_fn);
		(`Ötable) has (`rows`cols;(25Ätable%`_N;#%table));
		show `Ötable;
	}

	on_screen_update{table}:{
		sûtableÖtable%`_T;
		sû(~sÅ%`Ötable)/s;
		(%`Ötable)û(%`Ötable),s;
     		s has¡<(`titlefont;'helvetica10');
     		s has¡<(`font;'helvetica10');
		s has¡  <(`out;out_fn);
		(`Ötable) has (`rows`cols;(25Ätable%`_N;#%table));
		show `Ötable;
	}

ã Setup.

	s.AUTOWSû0	


