(if (= (unix-fork) 0)
    (do (print "Child here\n")
	(unix-exec "/bin/touch" '("touch" "foo" "bar") nil)
	(print "Oh shit!!!\n")))
(print "Sleeping\n")
(unix-usleep 10000000)
(if (unix-wait)
    (print "Waited\n"))
(if (unix-wait)
    (print "Oh bugger!!!\n"))
(print "Done\n")
