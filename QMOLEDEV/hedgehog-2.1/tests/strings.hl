;; Some random string tests

(print (hex "hedgehog") "\n")

(print (strstr "hedgehog" "eh") "\n")

(print (strrstr "hedgehog" "eh") "\n")

(print (strstr "hedgehog" "h") "\n")

(print (strrstr "hedgehog" "h") "\n")
