;; Test some special builtins

(print "available mem = " (available-mem) " bytes\n")
(print (unix-gettimeofday) "\n")
(print "available mem = " (available-mem) "\n")
(gc)
(print "available mem = " (available-mem) " bytes\n")
