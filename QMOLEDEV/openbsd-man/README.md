## OpenBSD man(1) utility

This repository contains a standalone version of the OpenBSD man(1) utility
for Linux. It contains all compatibility code to avoid adding extra dependencies
such as libbsd.

